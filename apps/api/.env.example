PORT=4000
NODE_ENV=development
MONGO_URI=mongodb://localhost:27017/handil
JWT_ACCESS_SECRET=replace_with_a_long_random_secret

# Production only — comma-separated list of allowed frontend origins.
# Leave empty in development (all origins allowed).
# Example: ALLOWED_ORIGINS=https://handil.app,https://www.handil.app
ALLOWED_ORIGINS=
