{
  "name": "handil",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=api",
    "api": "npm run dev --workspace=api",
    "mobile": "npm run start --workspace=mobile",
    "web": "npm run web --workspace=mobile",
    "dev:all": "concurrently -n api,web -c blue,green \"npm run api\" \"npm run web\""
  },
  "devDependencies": {
    "@types/react": "~18.3.0",
    "concurrently": "^10.0.3"
  },
  "overrides": {
    "@types/react": "~18.3.0"
  }
}
